#include <cmath>
#include <iostream>
#include <vector>

class matrix
{
    private:
        int test;
    public:
        matrix();
        ~matrix();
};

class math : matrix
{
    private:
    public:
        math() = default;
        ~math();
};
